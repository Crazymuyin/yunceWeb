const config = {
    // 启动端口
    port: 3400,

    // 默认数据库配置
    database: {
      DATABASE: 'yunce_web',
      USERNAME: 'root',
      PASSWORD: '123.coM&*(',
      PORT: '3306',
      HOST: '192.168.10.87'
    }
}

module.exports = config
